name: Deploy (servicess-app)

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  packages: write
  attestations: write

jobs:
  deploy:
    uses: pagopa/dx/.github/workflows/release-azure-containerapp-v1.yaml@bb7abcc5354751fc2e23608fd406effda931b1b9 # main
    name: Deploy
    secrets: inherit
    with:
      dockerfile_path: ./apps/services-app/Dockerfile
      dockerfile_context: .
      docker_image_name: pagopa/io-services-app
      docker_image_description: "Microservice that handles the services API."
      container_app: io-p-itn-svc-services-ca-01
      resource_group_name: io-p-itn-svc-rg-01
      environment: app-prod
