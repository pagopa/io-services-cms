name: Terraform plan multiregion

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    paths:
      - "infra/prod/**"
      - "infra/_modules/**"
      - ".github/workflows/infra-review-p.yml"

jobs:
  infra_review:
    uses: pagopa/dx/.github/workflows/infra_plan.yaml@8fd8da4ece3ea29908b983fd8960366efb914253
    name: Terraform plan
    secrets: inherit
    with:
      environment: prod
      base_path: infra
      use_private_agent: true
