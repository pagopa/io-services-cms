{
  "name": "services-app",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "main": "dist/main.js",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "build": "rm -rf ./dist & tsc",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**\"",
    "lint:fix": "eslint --fix \"src/**\"",
    "dev": "tsx --watch --env-file=.env --import ./src/instrumentation.ts ./src/main.ts",
    "dev:local": "tsx --watch --env-file=.env.local --import ./src/instrumentation.ts ./src/main.ts",
    "start": "node --import ./dist/instrumentation.js dist/main.js",
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/identity": "^4.13.0",
    "@opentelemetry/api-logs": "^0.218.0",
    "@pagopa/azure-tracing": "^0.5.5",
    "@pagopa/hexagonal-core": "^0.4.0",
    "@pagopa/hexagonal-fastify": "^0.2.0",
    "fastify": "^5.8.5",
    "neverthrow": "^8.2.0",
    "pg": "^8.16.3",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.15.5",
    "@vitest/coverage-v8": "catalog:",
    "@pagopa/eslint-config": "^4.0.6",
    "eslint": "^9.39.4",
    "prettier": "catalog:",
    "typescript": "^5.8.3",
    "vitest": "catalog:"
  }
}
