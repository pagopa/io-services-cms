{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "generate": {
      "outputLogs": "errors-only"
    },
    "build": {
      "inputs": [
        "src/**/*.ts"
      ],
      "outputs": [
        "dist/**"
      ],
      "dependsOn": [
        "generate",
        "^build"
      ]
    },
    "typecheck": {
      "inputs": [
        "src/**/*.ts"
      ],
      "outputs": [],
      "dependsOn": [
        "build"
      ]
    },
    "format": {},
    "lint": {
      "inputs": [
        "src/**/*.ts"
      ],
      "outputs": [],
      "dependsOn": [
        "build"
      ]
    },
    "test": {
      "inputs": [
        "src/**/*.ts",
        "src/**/*.tsx"
      ],
      "outputs": [],
      "outputLogs": "errors-only",
      "dependsOn": [
        "build"
      ]
    },
    "test:integrations": {
      "inputs": [
        "__integrations__/**/*"
      ],
      "outputs": [],
      "outputLogs": "errors-only",
      "dependsOn": [
        "build"
      ]
    },
    "coverage": {
      "inputs": [
        "src/**/*.ts",
        "src/**/*.tsx"
      ],
      "outputs": [
        "coverage/*"
      ],
      "outputLogs": "errors-only",
      "dependsOn": [
        "build"
      ]
    }
  }
}
