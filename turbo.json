{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "generate": {
      "outputMode": "errors-only",
      "dependsOn": [
        "^generate"
      ]
    },
    "build": {
      "inputs": [
        "src/**/*.ts"
      ],
      "outputs": [
        "dist/**"
      ],
      "dependsOn": [
        "generate",
        "^build"
      ]
    },
    "typecheck": {
      "inputs": [
        "src/**/*.ts"
      ],
      "outputs": [],
      "dependsOn": [
        "build"
      ]
    },
    "format": {},
    "lint": {
      "inputs": [
        "src/**/*.ts"
      ],
      "outputs": [],
      "dependsOn": [
        "build"
      ]
    },
    "test": {
      "inputs": [
        "src/**/*.spec.ts",
        "src/**/*.test.ts"
      ],
      "outputs": [],
      "outputMode": "errors-only",
      "dependsOn": [
        "build"
      ]
    },
    "test:integrations": {
      "inputs": [
        "__integrations__/**/*"
      ],
      "outputs": [],
      "outputMode": "errors-only",
      "dependsOn": [
        "build"
      ]
    },
    "coverage": {
      "inputs": [
        "src/**/*.spec.ts",
        "src/**/*.test.ts"
      ],
      "outputs": [
        "coverage/*"
      ],
      "outputMode": "errors-only",
      "dependsOn": [
        "build"
      ]
    }
  }
}
